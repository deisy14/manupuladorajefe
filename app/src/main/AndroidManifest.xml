<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ManipuladoraJ">

        <!-- PRIMERA PANTALLA: MENÚ DIARIO -->
        <activity
            android:name=".MenuDiarioActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- OTRAS PANTALLAS -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        <activity
            android:name=".InventarioActivity"
            android:exported="false" />
        <activity
            android:name=".PreparacionesActivity"
            android:exported="false" />
        <activity
            android:name=".ManipuladorasActivity"
            android:exported="false" />
        <activity
            android:name=".DetalleMenuActivity"
            android:exported="false" />
        <activity
            android:name=".ConfirmarMenuActivity"
            android:exported="false" />
        <activity
            android:name=".MenuAsignadoActivity"
            android:exported="false" />
        <activity
            android:name=".activity_perfil"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
    </application>

</manifest>